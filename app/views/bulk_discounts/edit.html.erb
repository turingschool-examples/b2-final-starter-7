<%= form_with url: edit_merchant_bulk_discount_path(@merchant), local: true do |f| %>
  <%= f.label :quantity_threshold %>
  <%= f.text_field :quantity_threshold %><br/><br/>
  <%= f.label :percent_discount %>
  <%= f.text_field :percent_discount %><br/><br/>
  <%= f.submit 'Submit'%>
<% end %>