<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { method: :post, url: merchant_discounts_path(@merchant, @discount), class: 'merchant_discount_form', local: true, model: nil } %>
