<!DOCTYPE html>
<html>
  <head>
    <title>Kaina's Esty Shop</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="top-container">
      <h1>Kaina's Esty Shop</h1>
      <div class="container">
        <% flash.each do |type, notice| %>
          <h5 class="text-info" style="background-color: yellow"><b><%= notice %></b></h5>
        <% end %>
      </div>
      <div class="container">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
